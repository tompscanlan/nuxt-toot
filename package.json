{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "start": "nuxt start",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "push": "amplify push",
    "pull": "amplify pull"
  },
  "devDependencies": {
    "@nuxt/devtools": "^0.7.1",
    "@types/node": "^18.17.0",
    "nuxt": "^3.6.5"
  },
  "dependencies": {
    "@aws-amplify/ui-vue": "^3.1.22",
    "aws-amplify": "^5.3.5"
  }
}
